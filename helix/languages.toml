[[language]]
name = "latex"
scope = "source.tex"
injection-regex = "tex"
file-types = ["tex"]
roots = [".git", ".latexmkrc"]
language-servers = [ "texlab" ]
auto-format = true
formatter = { command = "latexindent", args = ["-l", "-m",  "-"] }

[language-server.texlab.config.texlab]
# 1. Compile automatically on save
build.onSave = true
build.executable = "latexmk"
build.args = ["-pdf", "-interaction=nonstopmode", "-synctex=1", "%f"]

# 2. Tell TexLab to open and focus Skim at the exact line
build.forwardSearchAfter = true
forwardSearch.executable = "/Applications/Skim.app/Contents/SharedSupport/displayline"
forwardSearch.args = ["-g", "%l", "%p", "%f"]

diagnostics.ignoredPatterns = ["Unused entry"]


[[language]]
name = "python"

[language.auto-pairs]
'(' = ')'
'{' = '}'
'[' = ']'
"'" = "'"
'`' = '`'

[language-server.dafny-language-server]
command = "dafny"
args = [ "server" ]

[[language]]
name = "dafny"
scope = "source.dfy"
file-types = [ "dfy" ]
language-servers = [ "dafny-language-server" ]

[[grammar]]
name = "dafny"
source = { git = "https://github.com/hath995/tree-sitter-dafny", rev = "main" }
